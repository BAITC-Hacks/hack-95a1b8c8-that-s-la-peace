# Проверки backend

23.09.2026, Windows x64, Python 3.12, отдельное окружение .venv. Проверена новая серверная реализация в рабочем дереве dev-dimalarp поверх базового коммита 13a3707. Конкретный опубликованный SHA указывается в Git-истории и сообщении checkpoint, а не вписывается в содержащий сам отчёт commit.

- `python -m pip check`: зависимости согласованы.
- `python -m pytest -q`: первый полный запуск — 75 passed; 1 предупреждение Starlette о будущем предпочтении httpx2. Ошибок проверок нет. Дополнительные проверки и чистая копия будут зафиксированы ниже после выполнения.
- Настоящий сервер `python run.py`, loopback 8000: /api/health возвращает ok и 66.
- `python scripts/smoke.py`: PASS; плотная категория 6 и 4 допустимых на двух датах, редкая 2, оба пустых исхода, повторяемость и проверяемый совет.
- Измерение одного HTTP-запроса подбора на этой машине: 7.74 мс. Это единичный замер, не нагрузочный benchmark или гарантия задержки.
- Top-3 на 15 октября: HK-44733, HK-44923, HK-77838; на 16 октября: HK-44733, HK-88430, HK-44923. Видимые карточки меняются из-за календаря.

Проверены фильтры и их пересекающиеся причины, строгие типы и ошибочный JSON, границы даты, пропущенные/null optional, неправильный CSV и недоступность файла, происхождение карточек, независимость порядка от строк, минимальные альтернативы, изоляция static на отдельной тестовой папке.

NOT RUN: полный интерфейс друга, браузерный сценарий его приложения, независимая человеческая проверка, интернет-деплой и платформа сдачи. Unit-тест раздачи временного HTML не является проверкой настоящего frontend. Чистая установка из опубликованной копии ещё предстоит.

Итоговый повтор после правок объяснений и пяти дополнительных пограничных сценариев: **80 passed**, 1 предупреждение Starlette, 2.40 с. Включены безопасный 503 при внутреннем сбое, обе границы советов дат, видимая смена top-3 и различимость объяснений без имён.
