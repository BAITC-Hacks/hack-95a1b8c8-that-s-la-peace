# Состояние проекта

23.09.2026, checkpoint к 14:45. Общая сборка объединяет dev-dimalarp 7464685 и dev-zoro 4a9d305. Точные SHA, процессы, команды и результаты — [актуальная проверка](integration/20260923-1435.md). Публикации main/integration разрешены пользователем каждые 45 минут от 12:30; [расписание](RELEASE_SCHEDULE.md). Факт текущего push сверяется с remote; старые сообщения NOT RUN относятся к указанным в них историческим SHA.

Работают ввод, все фильтры, исходный каталог из 66 профилей, до трёх карточек, разные пустые исходы и детерминированный порядок. Дополнения: факты для сравнения и проверенные альтернативы даты/бюджета по явному нажатию. Выдержки музыкальных групп стали конкретнее, первая часть объяснения — короче. API v1, сортировка и исходный CSV не менялись.

Проверки candidate: 86 backend + 11 Node PASS; наш живой браузер — 15 запросов PASS, скрипт друга — 23 проверки/14 запросов PASS, fixtures отдельно PASS. В этом прогоне 43–265 мс, без JS-ошибок и горизонтального переполнения на 390 px. Новая .venv с фиксированными зависимостями установлена.

Проверяемый candidate запущен на http://127.0.0.1:8004. Обычный запуск полученной копии по README открывает http://127.0.0.1:8000. Интернет-публикации нет. Нужны ручное чтение карточек участниками, чистый запуск нового общего SHA другом и репетиция демо; после этого обновить его приёмку. Следующий срок main — 15:30, подготовка — 15:20. Подача платформы отдельная и ещё не подтверждена.
