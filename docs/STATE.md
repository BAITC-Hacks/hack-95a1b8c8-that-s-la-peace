# Состояние проекта

23.09.2026, первый час. Ветка backend: dev-dimalarp; base общего пакета 13a3707. Реализованы загрузчик каталога, API v1, все фильтры, детерминированное ранжирование, объяснения на фактах и проверенные suggestions. Датасет 66 профилей не изменён.

API запущен локально на http://127.0.0.1:8000. Проверки: итоговый полный pytest 80 passed; живой HTTP smoke PASS. Чистая установка и итоговый повтор проверок фиксируются в TEST_REPORT по факту выполнения. Основная команда Windows: `.venv\Scripts\python.exe run.py`; зависимости через requirements.txt.

Frontend друга пока не получен. Поэтому `/` отвечает 503 с пояснением, API работает, полноценный пользовательский интерфейс НЕ ПРОВЕРЕН. Не публиковать это состояние в integration как готовое приложение. Друг может получить backend из dev-dimalarp для проверки, либо передать готовый frontend SHA для первой общей candidate-сборки.

Следующие действия: подтвердить чистый запуск backend из опубликованной копии; получить имя ветки и SHA друга; собрать отдельный candidate, проверить настоящий UI с API; только после успеха обновить integration. Main не менять без отдельного указания.

Блокер общей сборки: отсутствует frontend/handoff друга. У backend нет обязательных ключей/платных аккаунтов/внешней модели. Деплой, проверка человеком и подача платформы не выполнены.
