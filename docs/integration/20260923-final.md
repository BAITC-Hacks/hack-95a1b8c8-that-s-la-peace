# Финальная общая проверка — 23.09.2026

## Версии и изменения

- Предыдущая main: f3e307f51ecb61778f2205e5aa4fcd292a63582d, опубликована 16:46:05.
- Принятый Ilyas: **d48a3783d3d75502bfb118492963de6819baa3bc**, готовый код 33fc818b689d344c4ab542749dcfeda8d0c6a145. Компактные списки, единая дата, ценовой ориентир и уточнение календаря сохранены.
- Наша финализация названия/README: c243f0b462519784a5c34629d8ff04f425b73c50.
- Полный проверенный код: **9be780001d993307d2daf09de1eb9a2363b87130**. Обычные merge сохранили обе истории. Два конфликта только в COMPLIANCE разрешены предметно: сохранены свежие доказательства Ilyas и новое поручение о безопасной очистке.
- Рабочая копия: `.integration-candidates/final-20260923-1712`; сервер 8017, launcher 14828, listener 22892. Проверка началась после изменения названия и получения свежего Ilyas.
- Последующие изменения — README/отчётные документы. Перед публикацией проверяется отсутствие изменения приложения, тестов и зависимостей относительно указанного кода. Итоговый hash отчётного коммита сверяется через remote, не записывается внутрь самого себя.

Название пользователя в HTML title, шапке, footer и API изменено на «Умный подбор подрядчиков»; локализация сохранена. Перенос длинного имени исправлен одной CSS-правкой. GitHub URL, авторство, история и исходное ТЗ не переименованы. API v1, CSV, matcher, schemas и catalog не менялись. B09 не выполнялся. Добавлена регрессия публичного имени API. README переписан для первого запуска экспертом и содержит будущие идеи отдельно от сделанного.

## Реальные результаты, 17:18–17:21 UTC+05:00

| Проверка | Результат |
| --- | --- |
| Полный backend pytest на финальном коде | **231 PASS**, 80.68 с, одно прежнее предупреждение Starlette/httpx |
| Node API/server/combobox | **12 PASS**; компонент использует отдельные синтетические варианты |
| Генератор catalog-guide --check | PASS: 66 профилей, 112 групп, 100 дат, SHA исходного CSV |
| HTTP smoke candidate | PASS, 13.46 мс один замер; 6/4/2, обе пустые причины, оба совета и повтор порядка |
| Наш browser-live | **20 настоящих запросов PASS**, 77–307 мс до результата; ошибок JavaScript и внешних запросов нет |
| Ilyas live-check | **23 проверки / 14 настоящих запросов PASS** |
| UX | **16 проверок / 10 запросов PASS**; задержка настоящего ответа и отказ справки помечены как симуляции |
| Network recovery | **5 PASS**, offline только в собственном browser context |
| Fixture browser | PASS отдельно; синтетические ответы не выдаются за реальный API |
| Название, языки и размеры экрана | **9 PASS**: 360/390/1440 px × RU/KK/EN; нет наложений/overflow/JS ошибок/CSP violations; PNG просмотрены агентом |
| Защита на новом чистом сервере | PASS: восемь публичных ресурсов + CSP, служебные файлы 404, объявленный/потоковый body больше 16 КиБ → 413, следующий подбор → 200, title API, /docs доступен |
| Исходники и данные | 39 изменённых текстовых файлов проверены эвристикой известных секретов; совпадений нет. CSV, matching.py, schemas.py, catalog.py неизменны |

Команды из candidate (окружение Python основной копии установлено отдельно):

```powershell
python -m pytest -q
node --test tests/ui/api.test.mjs tests/ui/server.test.mjs tests/ui/search-select.test.cjs
python frontend/generate-guide.py --check
python scripts/smoke.py --base-url http://127.0.0.1:8017
node tests/integration/browser-live.cjs http://127.0.0.1:8017
node tests/ui/live-check.cjs http://127.0.0.1:8017
node tests/ui/ux-check.cjs http://127.0.0.1:8017
node tests/ui/network-recovery.cjs http://127.0.0.1:8017
node tests/ui/browser-check.cjs http://127.0.0.1:8017
```

Фактический Python: `hack-95a1b8c8-that-s-la-peace/.venv/Scripts/python.exe`, 3.12.10. Node 25.2.1, Playwright 1.62.1, Edge 153.0.4234.48. Для browser задан PLAYWRIGHT_MODULE на установленный модуль; для артефактов — отдельный UI_CHECK_OUTPUT. Скрипты работали из candidate, не из старого сервера 8000.

## Чистая установка и повторный запуск

Свежий clone официального remote в `.verification/clean-final-20260923-1719`, выбран опубликованный exact 9be7800 из личной ветки до общей публикации. Новая venv командой `py -3.12 -m venv .venv`, только `pip install --no-cache-dir -r requirements.txt`, `pip check` PASS. Скрытые файлы/ключи из рабочей папки не копировались.

Сервер 8019: launcher 23352 / listener 32480. HTTP smoke 20.89 мс, наш browser 20, UX 16 и network 5 PASS. После остановки собственного дерева процессов и повторного запуска в 17:21: launcher 34836 / listener 36048, HTTP smoke 12.23 мс; полный JSON рекомендации совпал с исходным ответом по всем значениям и порядку карточек. Git status чистый. Backend pytest в чистой production venv повторно не устанавливался: полный тестовый прогон выполнен отдельно на том же коде.

## Границы и передача

Проверка техническая, выполнена агентами. Новый человеческий просмотр, настоящий screen reader, браузерный zoom 200%, физическое присутствие и доступ реального эксперта в приватный GitHub **NOT VERIFIED**. Локальная эвристика не доказывает отсутствие любого возможного секрета. Интернет-деплоя нет; localhost доступен только на этой машине. Подача формы не подтверждена.

Почасовые слоты — вне текущего кейса и данных: известны только busy_dates, часы нельзя выдумывать. Редактирование цены/ранжирования ради картинки не выполнялось. Новое присланное исследование просмотрено как список идей; большинство малых UX-уточнений уже включено Ilyas, исследование повторно не запускалось.

Доказательства: [сводка](evidence/20260923-final-checks.json), [browser](evidence/20260923-final-ours.json), [UX](evidence/20260923-final-ux.json), [чистая установка](evidence/20260923-final-clean-install.json), [защита](evidence/20260923-final-security.json), [название/экраны](evidence/20260923-final-branding.json), [исходники](evidence/20260923-final-source.json). Остальные результаты с тем же префиксом сохранены рядом. Публикация и очистка фиксируются после фактических действий.
