# Проверенный пакет к 17:00 — 23.09.2026

Подготовка выполнена заранее; живые проверки — в 16:44 UTC+05:00. Изменены README и документы продолжения; код приложения не менялся.

## Состав

- Предыдущие опубликованные main/integration: `4be77203d7f97a8e01f7ea8922635b78e650f8c7`, публикация 16:31:14.
- Наша часть: `543d8090e2ccc8aede27258abb43b4360e2becbe`, dev-dinmukhammed.
- Последний готовый Ilyas: `9eb9b12b6d3d809f88e8788709e89a295065c051`, dev-zoro. Уже входит в предыдущую общую версию. Новый дизайн по будущему аудиту пока не передан и не включён.
- Проверенный общий кандидат: `dc2677a31e665a7c7ae64b8c3fc2892bc59364d7`. Обычный merge в отдельной копии `.integration-candidates/20260923-1650`, обе общие истории и выбранные личные части сохранены.
- Последующие изменения этого пакета — только настоящий отчёт и документы. Итоговый опубликованный hash проверяется через `git ls-remote --heads origin main integration dev-dinmukhammed`; hash документа не записывается внутри самого себя.

README теперь содержит дальнейшие улучшения, отдельно от уже сделанного: объяснения, перевод содержания, сохранение/сравнение, проверка удобства с людьми, обновление каталога, состав цены, портфолио, публичное демо, исследование свободного ввода и большего каталога. Указаны необходимые реальные данные и границы текущего продукта. Это планы, не новые функции и не изменение B09.

## Фактическая проверка

| Действие | Результат |
| --- | --- |
| Сравнение с полным проверенным e70b3be по backend/frontend/data/scripts/tests/run.py/requirements | Изменений кода нет |
| Новый сервер из candidate, порт 8014, launcher 29548 / listener 19476 | Запущен именно candidate; используется установленное окружение основной рабочей копии |
| HTTP smoke через настоящий API/CSV | PASS; 66 профилей, плотная группа 6/4, редкая 2, обе пустые причины, два реально применимых совета, повтор порядка; 12.32 мс один запрос |
| Browser-live | PASS; 20 настоящих запросов, 57–251 мс до отображения, ошибок браузера и внешних запросов нет; включая человеческие замечания B03 и ширину 390 px |
| Полный backend pytest и Node | В этом пакете повторно не запускались: код/тесты/зависимости не менялись. Предыдущие 230 backend и 12 Node PASS относятся к e70b3be, отчёт 1620 |
| Чистая установка | В этом пакете заново не выполнялась; предыдущая чистая установка и перезапуск e70b3be остаются применимыми к неизменившемуся приложению |

Команды из рабочей копии candidate:

```powershell
& 'D:\HackAI\Thats La Peace team project\hack-95a1b8c8-that-s-la-peace\.venv\Scripts\python.exe' run.py --port 8014
& 'D:\HackAI\Thats La Peace team project\hack-95a1b8c8-that-s-la-peace\.venv\Scripts\python.exe' scripts/smoke.py --base-url http://127.0.0.1:8014
$env:PLAYWRIGHT_MODULE = 'C:\Users\johan\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules\playwright'
node tests/integration/browser-live.cjs http://127.0.0.1:8014
```

Результаты: [checks](evidence/20260923-1650-checks.json), [browser](evidence/20260923-1650-browser.json). Предыдущая полная приёмка — [1620](20260923-1620.md).

## Что дальше

Перед push повторно сверить remote и отсутствие новых конфликтующих изменений. После push подтвердить совпадение main/integration и своей ветки. Готовый новый дизайн Ilyas, если появится позже, принимать отдельным проверенным пакетом; его ветку не изменять. Следующая подготовка 17:35, публикация до 17:45. Пятый час ещё не заявлен завершённым. Человеческая репетиция и подача формы остаются неподтверждёнными.
