# Общая сборка к 15:30 — 23.09.2026

## Версии и процесс

- Общая база main/integration: fb79c6658a3e6db3785c759294563874ec7a8dba.
- Dinmukhammed, рабочая dev-dinmukhammed: c84d21901f5bc7bf4fe2408a365ff2a6753f4186 (код 6e67d74483548e60d42c6c5cc27be6e38e670ae4, уточнённая живая регрессия c84d219).
- Ilyas, опубликованная dev-zoro: 9be1df2c09656ed078d173e00fff5d64c88d1d77; включает UI-код 6f38144 и его новые документы. Dev-ilyas при последнем fetch ещё отсутствует; ветки Ilyas не менялись.
- Runtime candidate: cd0970f7e4d23008637122fc1ce945c85f9a8fa3. Следующий merge e62958fb079035a86162ed950286f7ee052f3e4d меняет только нашу живую проверку; diff runtime/backend/frontend/data/зависимостей между ними пустой.
- Worktree: D:/HackAI/Thats La Peace team project/.integration-candidates/20260923-1520.
- Сервер candidate: http://127.0.0.1:8010, launcher PID 28324, слушающий Python PID 27832. Запущен из новой .venv этой рабочей копии, не из старого процесса 8000.
- Обычные merge сохранили обе истории и прошли без конфликтов. Git author/email сохранены. Корневой AGENTS и README разделяют реальные роли и переход имён веток.

## Что исправлено

B01: цельные исходные блоки, без смешения пакетов; укороченная выдержка не используется как основание, чтобы не потерять позднее отрицание/доплату. B02: структурированные факты вместо рекламы; исходная цитата остаётся отдельной. Для состава/пакета явно сказано, что его стоимость нужно уточнить. B05: allowlist пяти публичных файлов плюс /, служебные/скрытые файлы недоступны; честный 503 и полезная локальная диагностика. B06: smoke требует оба ожидаемых совета, проверяет ровно одно изменённое поле, версии набора и реальный положительный ответ. B03: семь примеров source→explanation в EXPLANATION_REVIEW; решение по тексту пользователь делегировал агенту, человеческая приёмка этим не подтверждается.

## Реальная проверка 15:19–15:25 UTC+05:00

| Проверка | Результат |
| --- | --- |
| Новая .venv, установка requirements-dev.txt и pip check | PASS; фиксированные зависимости установлены, конфликтов нет; использован кеш pip |
| Полный pytest на final runtime cd0970f | 128 PASS, 3.40 с; одно прежнее предупреждение Starlette/httpx |
| Node API + HTTP | 11 PASS, 0 FAIL на собранном candidate |
| scripts/smoke.py по настоящему HTTP | PASS, 23.14 мс один замер; 6/4/2, обе пустые причины, два обязательных совета и их повторные запросы |
| Наш browser-live | PASS, 17 настоящих запросов, 43–277 мс; B01/B02 и уточнение цены видны в UI |
| live-check Ilyas | PASS, 23 проверки / 14 запросов, 68–231 мс; оба declared SHA = cd0970f |
| network-recovery Ilyas | PASS, 5 проверок: настоящий API до/после offline в изолированном browser context |
| UI desktop и 390 px | Без JS-ошибок, внешних запросов и горизонтального переполнения; desktop-снимок просмотрен агентом |
| Служебные файлы | Регрессии проверяют 404, фиктивный .env и реальные публичные ресурсы/MIME; реальные ключи не использовались |
| Исходный CSV, фильтры и ранжирование | CSV SHA-256 сохранён; соответствующие функции ранжирования/фильтрации не менялись, прежние проверки порядка проходят |
| Известные сигнатуры секретов | Совпадений нет в просмотренном кодовом пакете; эвристическая проверка, не гарантия для любых форматов |

Первый запуск нашего browser-live остановился на прежнем предположении, что каждое объяснение обязано включать цитату. Это противоречило согласованному B02. Проверка обновлена: либо дословная цитата, либо точные языки/часы профиля, при этом исходная выдержка отдельно совпадает с UI. Добавлены настоящие запросы B01/B02; повторный полный browser-live прошёл. Runtime при этой правке проверки не менялся. FAIL не скрывается и не относится к последнему прогону.

## Повторение

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe run.py --port 8010
# Второй терминал:
.\.venv\Scripts\python.exe scripts/smoke.py --base-url http://127.0.0.1:8010
node --test tests/ui/api.test.mjs tests/ui/server.test.mjs
node tests/integration/browser-live.cjs http://127.0.0.1:8010
node tests/ui/live-check.cjs http://127.0.0.1:8010
node tests/ui/network-recovery.cjs http://127.0.0.1:8010
```

Среда: Windows, Python 3.12.10, Node 25.2.1, Edge 153.0.4234.48, Playwright 1.62.1. Для browser-скриптов PLAYWRIGHT_MODULE указывал установленный bundled Playwright, UI_CHECK_OUTPUT — отдельную папку каждого прогона; тестовые инструменты не являются runtime-зависимостями приложения. UI_FRONTEND_SHA/UI_BACKEND_SHA/UI_SOURCE_SHA фиксировали проверяемый cd0970f; источник процесса дополнительно установлен по worktree/порту/PID.

Сырые JSON без изменения: [наш browser](evidence/20260923-1520-ours.json), [Ilyas live](evidence/20260923-1520-ilyas.json), [восстановление сети](evidence/20260923-1520-network-recovery.json). Снимки и серверные логи локально в D:/HackAI/Thats La Peace team project/.verification/integration-20260923-1520.

## Ограничения и публикация

Человеческая оценка всех объяснений, screen reader, присутствие, доступ эксперта к GitHub и отправка формы не подтверждены. У некоторых фотографов одинаковая фактическая часть объяснения; различается цена — это явно отмечено в B03. Интернет-деплоя нет. Бронирования/рейтинги/LLM не заявлены.

После этого отчёта меняются только документы/сырые результаты; перед push сверяются свежие refs и предки. Опубликованный итоговый SHA и время подтверждаются remote после push. Следующее окно — 16:15, подготовка 16:05. Финальный комплект и FINAL_COMPLIANCE_REPORT готовятся к окончательной сдаче, текущий checkpoint финальным не объявляется.
