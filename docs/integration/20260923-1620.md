# Завершение backend и общий UX — 23.09.2026

## Проверяемые версии

- Общая опубликованная база: cfdf9c57bc9517153082f3bff103a47c86ecf411; окно 16:15 закрыто в 15:57:49.
- Dinmukhammed: HTTP-защита/категории/browser адаптация — 97398da13532e015922bbf292cac524388ca498a; личный checkpoint gate — 3bf1fe9. Ветка dev-dinmukhammed, прежняя dev-dimalarp не менялась.
- Ilyas: свежий ready dev-zoro 9eb9b12b6d3d809f88e8788709e89a295065c051; UI-код 6039476/abbf1bf. Handoff подтверждает готовность и чистый 6039476, но не новую общую сборку. Dev-ilyas отсутствовала при fetch.
- Общий runtime/UI candidate: **62bd7905850eeaaf0f36fc60b5428b9c75c4280d**.
- Полный кандидат с checkpoint/tests: **e70b3be0d2cf64b9a8e6a826920a8ed540b7b79c**. Diff backend/frontend/data/run.py/requirements между 62bd790 и e70b3be пустой.
- Оба обычных merge прошли без конфликтов. Backend-правка Ilyas — три разрешённых публичных файла; она объединена с HTTP middleware без замены чужого дерева.
- Candidate worktree: D:/HackAI/Thats La Peace team project/.integration-candidates/20260923-1620. Новый сервер 8012, launcher 31240, listener 29528. Старый 8000 не служил доказательством этих правок.

## Реализовано

1. ASGI ограничивает фактически полученные bytes до 16 384 включительно, до JSON parsing. Content-Length используется для раннего отказа, не как единственный счётчик. Превышение — 413 request_too_large без внутренних данных; обычные ответы API v1 сохранены.
2. nosniff/no-referrer на ответах, CSP только на публичных UI-файлах. Allowlist сохранён и явно дополнен i18n.mjs/search-select.mjs/catalog-guide.json. Swagger/ReDoc не получают frontend CSP.
3. Дополнительная техническая оценка B03: по два реальных профиля фотографов, видеографов, live-band, площадок и флористов. Это не новая человеческая приёмка; прежнее решение Dinmukhammed сохранено.
4. Одноразовый personal checkpoint, default check-only, отдельный --publish: exact origin/branch, Git state/remote, принадлежность файлов, secret heuristic, pytest, свой сервер/smoke, повторный snapshot, commit/push только проверенного SHA в личную ветку. Без main-publisher/второго расписания. Непубликованные локальные коммиты и binary changes требуют отдельного review.
5. Готовый UI Ilyas: поиск справочников, RU/KK/EN, ручная дата/формат бюджета, auto-mode, справочная статистика с проверкой SHA. В отдельной основной browser-проверке auto/filter toggles отключены, чтобы проверять ручные запросы и полную категорию; собственные UX-тесты проверяют новые режимы.

CSV, API v1, matcher/фильтры/порядок не изменялись. B01/B02/B05/B06 приняты повторным общим прогоном; B03 human PASS WITH REQUESTED CHANGES + отдельный технический повтор. B09 не выполнялся. Нет новых runtime-зависимостей, LLM/API-ключей или миграций.

## Выполненные проверки, 16:12–16:27 UTC+05:00

| Проверка | Результат |
| --- | --- |
| Новая venv candidate, установка requirements-dev без кеша, pip check | PASS |
| Полный backend на e70b3be | **230 PASS**, 56.33 с; одно прежнее предупреждение Starlette/httpx |
| Node API/server + search component | **12 PASS**, 0 FAIL; component использует помеченные synthetic fixtures |
| Генератор справки --check | PASS: 66 профилей, 112 групп, 100 дат/группу; SHA исходного CSV |
| HTTP smoke candidate | PASS, 22.42 мс один замер; 6/4/2, обе пустые причины, повтор порядка и два проверенных совета |
| Наш browser-live | **20 настоящих запросов PASS**, 62–348 мс submit-to-render |
| Ilyas live-check | **23 проверки / 14 запросов PASS** |
| Ilyas UX | **15 проверок PASS**, 10 реальных основных запросов; симуляции задержки и отказа справки явно помечены |
| Network recovery | **5 PASS**, offline только в browser context |
| Реальный HTTP security | PASS: завышенный body и chunked без Content-Length → 413; следующий обычный запрос → 200; headers/CSP/docs и private 404 |
| Отдельный просмотр UI/CSP | Desktop/390 px читаемы; securitypolicyviolation пуст; Swagger реально загрузился без ошибок |
| Личный checkpoint | **63 regression PASS**; реальный check-only 16:21 PASS (71.235 с с pytest/smoke); после cleanup собственных серверных процессов не осталось |
| Secret heuristic | Изменённые файлы cfdf9c5..e70b3be: 31 текстовый файл, совпадений нет; рабочий/staged пакет дополнительно проверяется до commit |
| Данные/подбор | CSV SHA-256 неизменен; matching.py совпадает с проверенным B03; источник цитат и порядок сохранены |

Первый Node-запуск search-select не нашёл модуль Playwright, потому что runner не задал путь к установленному bundled module. Установленные зависимости приложения не менялись; после задания PLAYWRIGHT_MODULE повтор всех 12 Node checks PASS. Этот начальный FAIL не скрыт и не относится к последнему прогону.

Независимый просмотр checkpoint обнаружил гонку push HEAD; исправлен push фиксированного проверенного SHA и добавлена регрессия. Проверка Windows cleanup обнаружила возможность оставить дочерний Python; теперь taskkill ограничен деревом собственного созданного PID, с регрессией и реальным проходом. Оба исправления повторно просмотрены независимым агентом, PASS.

## Чистая копия из GitHub

Свежий clone из официального GitHub в D:/HackAI/Thats La Peace team project/.verification/clean-final-20260923-1625. До общей публикации выбран exact e70b3be из уже опубликованной личной dev-dinmukhammed; проверены SHA и чистый status. Новая venv; `pip install --no-cache-dir -r requirements.txt`, pip check PASS. Никакие файлы/ключи из рабочей папки не копировались. Сервер 8013: launcher 32756/listener 6788, после остановки и повторного запуска launcher 27404/listener 15500.

Повторены настоящий HTTP smoke (10.78 мс один замер), 20 наших запросов, 23 проверки Ilyas / 14 запросов, 15 UX и 5 network-recovery — PASS. После перезапуска smoke PASS (30.83 мс) и полный JSON подбора совпал с ответом до остановки. Рабочее дерево клона осталось чистым. [Протокол установки](evidence/20260923-1620-clean-install.json).

Тестовая среда: Windows, Python 3.12.10, Node 25.2.1, Playwright 1.62.1, Edge 153.0.4234.48. Runtime требует только Python и requirements.txt; Node/Playwright используются для технических браузерных проверок. UI_*_SHA — заявленные runner значения, поэтому источник процесса дополнительно установлен по worktree/клону, порту и PID.

## Повторение

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --no-cache-dir -r requirements-dev.txt
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe frontend/generate-guide.py --check
node --test tests/ui/api.test.mjs tests/ui/server.test.mjs tests/ui/search-select.test.cjs
.\.venv\Scripts\python.exe run.py --port 8012
# Второй терминал; Playwright/Edge должны быть установлены:
.\.venv\Scripts\python.exe scripts/smoke.py --base-url http://127.0.0.1:8012
node tests/integration/browser-live.cjs http://127.0.0.1:8012
node tests/ui/live-check.cjs http://127.0.0.1:8012
node tests/ui/ux-check.cjs http://127.0.0.1:8012
node tests/ui/network-recovery.cjs http://127.0.0.1:8012
```

Сырые доказательства: [наш browser](evidence/20260923-1620-ours.json), [Ilyas live](evidence/20260923-1620-ilyas.json), [UX](evidence/20260923-1620-ux.json), [сеть](evidence/20260923-1620-network.json), [реальный HTTP](evidence/20260923-1620-http-security.json), [secret scan](evidence/20260923-1620-secret-scan.json), [визуальный/CSP](evidence/20260923-1620-visual-csp.json). Локальные полные логи/снимки — D:/HackAI/Thats La Peace team project/.verification/integration-20260923-1620.

## Границы и финальный комплект

Проверки выполнялись агентами. Новая человеческая приёмка, screen reader, физическое присутствие, доступ эксперта и форма не заявлены. /favicon.ico даёт необязательный 404, на основной сценарий не влияет. Secret scan эвристический; рейтинг в цитате видеографа — самоописание каталога, не внешняя верификация. Интернет-деплоя нет.

FINAL_COMPLIANCE_REPORT связывает требования, SHA, демо и оставшиеся действия капитана. После проверки меняются только документы/evidence; перед main повторный fetch и проверка обеих историй, после push — ls-remote. Legacy-ветки сохраняются. Расписание 17:00 и 17:45 остаётся; повторные бессодержательные коммиты не создаются, фактический финальный результат пятого часа фиксируется отдельно.

Дополнительные сырые результаты чистого клона: [наш browser](evidence/20260923-1620-clean-ours.json), [Ilyas](evidence/20260923-1620-clean-ilyas.json), [UX](evidence/20260923-1620-clean-ux.json), [сеть](evidence/20260923-1620-clean-network.json), [реальный checkpoint](evidence/20260923-1620-checkpoint.json).
