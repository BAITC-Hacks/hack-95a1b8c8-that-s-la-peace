# Финальная дополнительная проверка запуска для жюри

23.09.2026, 17:42–17:46 UTC+05:00. Прямое новое поручение: простой самостоятельный запуск; затем вернуть название **That's La Peace**. Runtime-исходники **f7a1980cf47dc77f2bb13115d974b0e00b3cc35f**. Предыдущая приёмка 9be7800 остаётся исторической. Ilyas d48a378/33fc818 по fresh fetch не изменился. API v1, CSV, фильтры и ранжирование не менялись; B09 не выполнялся.

## Поставленный пакет

START.cmd + portable/ContractorPicker.exe, 15 668 260 bytes, SHA256 `cb7d3b3188ac1f4f00ce0cc6c433a8c11c3b7d05a0e0434652788e7797972457`. Python 3.12.10 AMD64 и runtime внутри. PyInstaller 6.22.3; восемь публичных assets, исходный CSV, полные notices. [Происхождение/сборка](../../portable/README.md). Имя сохранено в RU/KK/EN, API и документах.

## Результаты

- **260 backend pytest PASS**, 58.44 s, включая 29 новых regression запуска; одно прежнее предупреждение Starlette/httpx, без падений. [XML](evidence/20260923-portable-pytest.xml).
- **11 Node tests PASS**, отдельная browser-проверка combobox PASS; guide из CSV PASS (66/112/100).
- Настоящий упакованный сервер: **20 browser submissions PASS**, без ошибок JS и внешних запросов; **16 UX PASS**, **5 network-recovery PASS**. В UX явно отмечены симуляции задержки и недоступности справки; ответы подбора настоящие.
- HTTP smoke PASS: 6/4/2 кандидата, два пустых состояния, оба применимых совета, стабильный порядок; 8.92 ms в одном локальном измерении.
- **9 branding checks PASS**: 360/390/1440px × RU/KK/EN, title/header/footer и отсутствие горизонтального переполнения. Мобильный снимок отдельно визуально просмотрен агентом.
- Чистая папка с пробелами и кириллицей: только START.cmd и portable, **без исходников/.venv**. PATH=System32, PYTHONHOME/PYTHONPATH ведут в несуществующие пути, PIP_NO_INDEX=1. Готовность с66 профилями за **3058 ms**.
- Остановка своего дерева и повторный запуск: **1809 ms**, полный декодированный JSON совпал, включая порядок. Первая попытка контроллера имела ошибку quoting cmd /c и не запускала продукт; повтор выполнен тем же PowerShell Start-Process, что первичный успешный запуск, отдельно записан в evidence.
- Независимый агент повторил чистый пакет с занятым8000: автоматически выбран55277; старый8000 остался жив. Процесс загрузил **python312.dll из _MEI**, а не системного Python. Собственное дерево остановлено.

## Воспроизводимость и границы

Команды запуска и проверки — основной README. Windows11 Pro64 10.0.26200, Ryzen7 4800H, около16GB; это среда проверки, не измеренный минимум. Сеть всей машины не отключалась: проверены отсутствие внешних запросов основного UI, отсутствие installer в запуске, изолированный browser offline/retry; /docs использует внешний SwaggerCDN. Браузерное автооткрытие проверено unit-тестами функции готовности, live-проверки выполнялись с --no-browser и отдельным Playwright. Проверка на другом физическом компьютере и всех версиях Windows не заявляется. EXE unsigned; обход защиты не предлагается.

Первоначально нужно получить репозиторий/папку; доступ жюри в закрытый GitHub и нажатие капитаном «Сдать решение» **NOT VERIFIED**. Наличие portable не делает localhost публичным. Автоматизация остаётся PAUSED. Организационные подтверждения и исходный human review B03 не переобозначены.

Доказательства: [чистый запуск](evidence/20260923-portable-launch.json), [повтор](evidence/20260923-portable-restart.json), [занятый порт/DLL](evidence/20260923-portable-busy-port.json), [browser](evidence/20260923-portable-browser.json), [UX](evidence/20260923-portable-ux.json), [network](evidence/20260923-portable-network.json), [название](evidence/20260923-portable-branding.json), [отдельный EXE smoke](evidence/20260923-portable-exe-smoke.json).
