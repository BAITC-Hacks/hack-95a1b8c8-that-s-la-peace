# Общий runtime с компактным UI — техническая проверка Ilyas

23.09.2026, UTC+05:00. Проверенный runtime `f88888641238944373da454d39dee2a051206136`: обычное объединение `1ddd3c2134ef3df6d4ba287c05c26c630c4c2ec3` и `f3e307f51ecb61778f2205e5aa4fcd292a63582d` в личной dev-zoro. Обе истории сохранены. Это локальная проверка общего кода, ещё не приёмка итоговой main.

Новый процесс запущен из worktrees/ilyas после объединения: порт 8016, PID 21428. Python 3.12.14, Edge 153.0.4234.48, Playwright 1.62.1. Использована существующая venv с requirements-dev; новая чистая установка этой версии здесь НЕ заявляется.

| Проверка | Фактический результат |
| --- | --- |
| `python -m pytest tests/backend -q` | PASS: 230, 66.43 с; одно предупреждение Starlette/httpx |
| `node tests/integration/browser-live.cjs http://127.0.0.1:8016` | PASS: 20 сценариев настоящего API, завершён 16:53:59; B01/B02/B03, смена даты, редкая категория, оба пустых исхода |
| `node tests/ui/ux-check.cjs http://127.0.0.1:8016` | PASS: 16 проверок, 10 настоящих запросов, 16:53:47–16:54:02 |

Для browser-live применён готовый frontend/integration-test.patch: выбор через видимый combobox и единое поле даты. Исходные проверки фактов/ID сохранены. Patch уже применён, повторно применять его не нужно.

Сырые результаты: [browser](evidence/20260923-1654-ilyas-browser.json), [UX](evidence/20260923-1654-ilyas-ux.json). UX отдельно маркирует симуляции задержки настоящего ответа и отказа справки; основной подбор не подменён fixtures.

`9be1df2` уже предок общей истории. HK-31819: полный большой состав, без оборванной фразы, цена расширенного состава требует уточнения. HK-25279: фактические русский язык/до 5 часов, рекламный текст не используется как объяснение. Повторная человеческая оценка исправлений NOT VERIFIED; данный проход выполнен агентом.

Остаётся: интегратор Dinmukhammed включает готовый dev-zoro в main/integration и подтверждает точный SHA; отдельная чистая приёмка нового UI; человеческая репетиция и реальный доступ эксперта. По текущему ответу Ilyas проверка формы отложена, отправка не выполнялась. Git push не означает сдачу.

## Продолжение: UX-исправления и чистая проверка 17:01–17:06

Опубликованный и повторно клонированный runtime: `9a3a446536a08f960ea7c93bc2ff955a71e81053`. Это общий код обоих участников в личной dev-zoro, не подтверждение публикации в main. Remote dev-zoro после push совпал с SHA. После f888886 backend, CSV и runtime-зависимости не менялись.

Четыре небольших изменения: ценовой ориентир перед бюджетом; честная подпись «начальная цена по каталогу» на RU/KK/EN; явная ссылка к готовой выдаче без автоматического переноса фокуса; казахские названия месяцев/дней вместо браузерных M10/Mon. Переход появляется только после ответа и скрывается при изменении формы. Исходные объяснения/цитаты не переводятся и не подменяются.

- Рабочая UX-проверка: 16/16, 10 настоящих запросов, 17:02:13–26 — [JSON](evidence/20260923-1702-ilyas-ux.json).
- Дополнительная доступность: 9/9, 17:01:48–53 — [JSON](evidence/20260923-1702-ilyas-accessibility.json). Полный путь исключительно клавиатурой до 3 реальных карточек; RU/KK/EN, 320 px, календарь/списки/результаты, расположение справки, подписи, переход и фокус. Обнаруженная при первом визуальном проходе ошибка KK исправлена и повторно просмотрена.
- 200% оценены только через эквивалент reflow 690 CSS px для ширины 1380. Настоящий масштаб браузера 200%, screen reader и проверка человеком — NOT VERIFIED.

### Новая установка из remote

Чистая папка `D:/HackAlem AI/acceptance/ux-9a3a446`, clone официального remote с веткой dev-zoro, затем detached exact SHA 9a3a446. Выбор dev-zoro вместо main — явное отличие от стандартного пункта README для приёмки перед передачей интегратору; остальные шаги установки/запуска выполнены по README. Никакие .env, кеши, данные, настройки приложения или браузерные сессии из рабочей папки не переносились. Clone использовал разрешённый доступ участника к GitHub; это не доказывает доступ организатора.

Команды после clone/выбора SHA:

```powershell
& 'C:\Users\Kazy\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m venv .venv
.\.venv\Scripts\python.exe -m pip install --no-cache-dir -r requirements.txt
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe run.py --port 8018
.\.venv\Scripts\python.exe scripts/smoke.py --base-url http://127.0.0.1:8018
```

Python 3.12.14, Windows/PowerShell; только фиксированные production requirements, новая venv и загрузка пакетов без кеша. pip check PASS. Для проверок отдельно использован имеющийся Playwright 1.62.1 / Edge 153.0.4234.48; пользователю приложения Node/Playwright не нужны. Новый непостоянный browser context без личной авторизации.

| Проверка чистого SHA | Факт |
| --- | --- |
| HTTP smoke | PASS, 11.94 мс один замер, 66 профилей, обе даты/пустые исходы/оба совета/повторяемость |
| browser-live | PASS, 20 реальных сценариев; 0 browser errors и 0 внешних запросов — [JSON](evidence/20260923-1705-ilyas-clean-browser.json) |
| UX | PASS, 16/16, 10 реальных запросов, 17:04:55–17:05:09 — [JSON](evidence/20260923-1705-ilyas-clean-ux.json) |
| Остановка/повтор | Первый собственный PID 15124 остановлен; повторный PID 21780, тот же порт 8018; полный JSON D1 совпал, smoke PASS 8.09 мс в 17:06 |
| Чистота копии | `git status --short` пустой после всех проверок; HEAD exact 9a3a446 |

230 backend-тестов из первого среза покрывают неизменённый backend; новый полный запуск pytest на production-only venv не заявляется. После этого пакета меняются только документы/сырые доказательства, их commit будет отдельным.

### Просмотр объяснений агентом

HK-31819: полный блок большого состава заканчивается словом «звукорежиссёр»; объяснение не обрывает смысл и отдельно требует уточнить стоимость большого состава. HK-25279: объяснение использует русский язык и до 5 часов; рекламное «сверкаем» остаётся только честной цитатой исходного описания, а не основанием рекомендации. Подбор/порядок не изменены. Это технический просмотр агента, не повторное человеческое принятие B03.

Промпт для друга и исследовательские основания: [DESIGN_PROMPT_FOR_FRIEND](../handoffs/DESIGN_PROMPT_FOR_FRIEND.md). Основные незавершённые действия: Dinmukhammed объединяет готовый dev-zoro с main/integration и фиксирует сдаваемый SHA; человек проходит демо и подтверждает доступ эксперта. Проверку формы пользователь отложил; подачи не было.

## Последнее уточнение календаря — 17:13

Runtime `33fc818b689d344c4ab542749dcfeda8d0c6a145` опубликован и подтверждён в dev-zoro. После 9a3a446 изменены только два UI-файла: короткое пояснение на RU/KK/EN, что число относится к выбранной услуге и дату с 0 можно выбрать; при нуле на выбранную дату поясняется отсутствие времени бронирований. Алгоритм, календарные ограничения ТЗ, API, CSV, зависимости и стили не менялись.

Проверены все 66 профилей / 3337 busy_dates: только даты, временных интервалов нет. max_hours — максимум присутствия, не начало/конец брони. ТЗ, п. 3, требует исключать конкретного занятого на дату подрядчика. Изобретать свободные часы или освобождать того же человека при другой категории нельзя.

Живой UI: Астана, свадьба, 15.10, 1M, Флорист — день с 0 enabled и нажат, ответ no_matches; меняется только услуга на Ведущий — тот же день, matched и IDs HK-37181/HK-80581/HK-97041. Обе новые подсказки проверены RU/KK/EN. [Сырой протокол](evidence/20260923-1710-ilyas-availability.json).

В изолированной копии получен exact 33fc818 из remote; production venv предыдущей чистой установки сохранена, зависимости не менялись, pip check PASS. Это продолжение чистой приёмки, не ещё одна новая установка. Сервер перезапущен из exact checkout: порт 8018, PID 16656. [UX 16/16, 10 реальных запросов](evidence/20260923-1713-ilyas-clean-ux.json), 17:13:16–29, PASS; HTTP smoke PASS (8.32 мс один замер), status чистый. Последующие документы не меняют проверенный runtime.

Почасовой подбор — зависимое будущее расширение: нужен разрешённый источник интервалов и согласование с ТЗ/владельцем API. Это не блокирует выполненный обязательный подбор по датам. Новый общий main/integration и подача не подтверждены этим личным push.
