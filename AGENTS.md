# Совместная работа That's La Peace

Прямые указания участников имеют приоритет над старыми планами. Перед работой прочитай docs/STATE.md, docs/BACKEND_TASKS.md, docs/API_CONTRACT.md, docs/RELEASE_SCHEDULE.md, docs/COMPLIANCE.md и персональный промпт. Исторические отчёты относятся к указанным SHA.

## Роли и ветки

- Dinmukhammed: backend/, data/, scripts/, tests/backend/, tests/integration/, запуск, зависимости, README и общие документы. Единственный интегратор и публикующий main/integration.
- Ilyas: frontend/, tests/ui/, DEMO, EXPERT_CHECK и свой handoff. Дизайн и независимая техническая приёмка. По handoff пакета 6f38144 Ilyas также капитан; отправка формы остаётся отдельным подтверждаемым действием человека.
- Рабочая ветка Dinmukhammed с 23.09.2026 около 15:10 — dev-dinmukhammed, upstream origin/dev-dinmukhammed. Прежняя origin/dev-dimalarp сохранена; новую работу и push туда не вести.
- Последняя подтверждённая ветка Ilyas — dev-zoro. Переход на dev-ilyas выполняет и подтверждает только Ilyas. Перед получением части сверяй свежий handoff и remote. Не создавать, не переключать и не пушить его ветки из задачи Dinmukhammed.
- Эти имена относятся к разным ролям: не навязывать обоим участникам одну рабочую ветку. Не менять Git author/email, прошлые коммиты и SHA ради человеческих имён.

## Параллельная работа

У каждого участника своя рабочая копия. Перед Git-переключениями проверить status, worktree и активную работу; не переключать общий checkout параллельно другому исполнителю. Не удалять локальный прогресс. Подзадачи внутри одной сессии получают непересекающиеся файлы; Git ведёт только интегратор.

Ilyas может передать полезную backend-идею/реализацию из своей ветки с точным SHA и описанием API. Пересекающиеся правки заранее обозначить; финальный выбор, адаптация и проверка — в candidate интегратора. Не копировать целиком старое дерево участника, не разрешать конфликт массовым ours/theirs.

## Публикации

Сроки 23.09.2026 UTC+05:00: 15:30, 16:15, 17:00, 17:45; подготовка за 10 минут, прекращение расширения за 15 минут. После 18:00 оцениваемую версию не менять. Действует один heartbeat hackalem-main-45 в задаче интегратора; второй таймер/публикующий процесс не создавать. Перед действием проверить журнал и remote, чтобы не повторить выполненный цикл.

Commit/push своей ветки и проверенной общей версии в main/integration разрешены. Сохранять историю обеих общих веток; обычные merge в отдельном candidate. Перед push повторный fetch, после push сверка remote SHA/предков. Без force-push, reset --hard и переписывания истории. Не публиковать сломанное ради времени.

## Качество и отчётность

Исходный CSV, жёсткие фильтры, API v1 и порядок карточек сохранять при исправлении объяснений. Источник цитировать честно; AI-работу, автоматические проверки и человеческую оценку отмечать отдельно. Предусмотренные пользователем регрессии, HTTP smoke и реальный UI/API проверять на коде candidate, а не на старом сервере. Статика разрешает только публичные ресурсы приложения. Проверки доступа к .env используют лишь фиктивные данные.

Обновлять существующие BACKEND_TASKS, STATE, PROGRESS и единственную COMPLIANCE по факту; для финальной версии подготовить FINAL_COMPLIANCE_REPORT. Push не означает отправку формы. Новые имена корректировать только в актуальных инструкциях; CSV, сохранённые протоколы, цитаты и старые отчёты сохранять.
